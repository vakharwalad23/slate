# SwiftUI helper

> Placeholder. The native Swift menu-bar helper (`helpers/mac-helper`) begins in **M2**. This
> doc is populated when that work starts. Coding standards already live in
> `.claude/rules/swiftui.md`.

Planned scope (from `plan.md`): `MenuBarExtra` UI, `NWListener` WebSocket server, Bonjour via
the dns-sd C API, app enumeration + icon extraction (`NSWorkspace.icon(forFile:)` -> 256px PNG),
pairing + token store, Accessibility detection. Target: macOS 13+, Swift 6.2, non-sandboxed /
notarized-only.
